
export class RootDTO {
constructor({ id: number, userName: string, isActive: boolean }: { id: number, userName: string, isActive: boolean }) {
  id = id;
    userName = userName;
    isActive = isActive;
}
}
